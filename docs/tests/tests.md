# Test strategy

Use unit tests for pure application and service logic, architecture checks for dependency and
ISR rules, and target-level tests for hardware integration. Each test command must publish a
machine-readable report under `reports/latest/`.
