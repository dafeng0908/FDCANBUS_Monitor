# Coding style

Use C11 for firmware modules. Keep functions focused, use explicit fixed-width types at
hardware and protocol boundaries, and treat compiler warnings as defects. Public interfaces
must describe ownership, error handling, and ISR safety where relevant.
