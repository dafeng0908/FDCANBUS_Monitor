# Folder rules

Generated STM32 content remains under `firmware/FDCAN_TOOL_cmake/`. Hand-written firmware
modules belong to the BSP, Services, or App layer they implement. Build outputs and harness
reports are generated artifacts and must not be committed.
