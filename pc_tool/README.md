# PC Tool

## Status

This directory is reserved for the future Python Qt6 CAN FD host application. No host
application implementation is included yet.

## Planned boundary

The PC tool will consume versioned transport messages from firmware and own presentation,
operator actions, and local persistence. It must not encode STM32 HAL assumptions or depend
on firmware source paths.
