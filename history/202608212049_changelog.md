# Change record — 2026-08-21 20:49 (Asia/Taipei)

## Updated items

- Updated `tools/doctor.py` so development-tool checks execute and record each tool's
  `--version` command output.
- Updated the configured ARM GNU Toolchain check to validate the configured compiler binary
  with `--version`, with a PATH fallback that receives the same validation.
- Updated the active execution plan with the doctor verification evidence.
